import Link from "next/link";
import { requestRegistrationCode } from "@/app/(auth)/actions";
import { TurnstileWidget } from "@/components/auth/turnstile-widget";

export function RegistrationForm() {
  return <form action={requestRegistrationCode} className="mt-8 grid gap-5"><label className="field">Correo electrónico<input className="input" name="email" type="email" autoComplete="email" required /></label><label className="field">Contraseña<input className="input" name="password" type="password" autoComplete="new-password" minLength={12} required /></label><label className="field">Confirmar contraseña<input className="input" name="confirmPassword" type="password" autoComplete="new-password" minLength={12} required /></label><p className="text-xs leading-5 text-slate-500">Usa al menos 12 caracteres, mayúscula, minúscula, número y símbolo. Después te enviaremos un código para confirmar tu correo.</p><label className="flex items-start gap-3 rounded-2xl border border-slate-200 bg-slate-50 p-4 text-sm leading-6 text-slate-700"><input type="checkbox" name="acceptTerms" value="yes" className="mt-1 size-4 shrink-0 accent-blue" required /><span>He leído y acepto los <Link className="font-bold text-blue underline underline-offset-2" href="/terminos" target="_blank" rel="noreferrer">Términos y Condiciones</Link> de TramitexFederal.</span></label><TurnstileWidget /><button className="button h-12" type="submit">Continuar y verificar correo</button></form>;
}
