"use client";
import { FormEvent, useState } from "react";
import { ArrowRight, CheckCircle2, Search } from "lucide-react";

export function TrackingForm() { const [folio, setFolio] = useState(""); const [searched, setSearched] = useState(false); function submit(event: FormEvent) { event.preventDefault(); setSearched(true); } return <><form onSubmit={submit} className="mt-8 flex flex-col gap-3 sm:flex-row"><label className="relative flex-1"><Search className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-400" size={19} /><span className="sr-only">Folio de trámite</span><input className="input h-13 pl-11" value={folio} onChange={(event) => setFolio(event.target.value.toUpperCase())} placeholder="Ej. TRM-2026-00001" required /></label><button className="button h-13" type="submit">Consultar <ArrowRight size={17} /></button></form>{searched && <div className="mt-7 rounded-2xl border border-blue-100 bg-blue-pale p-5"><div className="flex gap-3"><CheckCircle2 className="mt-0.5 text-blue" /><div><p className="font-semibold text-navy">Consulta de demostración</p><p className="mt-1 text-sm leading-6 text-slate-600">El seguimiento real se conectará cuando el sistema administrativo esté listo. Nunca mostraremos documentos ni información personal en esta vista.</p></div></div></div>}</>; }
