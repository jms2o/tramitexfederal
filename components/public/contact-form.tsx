import { services } from "@/lib/data/services";
import { submitContactRequest } from "@/app/(public)/actions";

export function ContactForm() { return <form action={submitContactRequest} className="grid gap-5 sm:grid-cols-2"><label className="field">Nombre<input className="input" name="name" required /></label><label className="field">Teléfono<input className="input" name="phone" type="tel" required /></label><label className="field">Correo<input className="input" name="email" type="email" required /></label><label className="field">Tipo de cliente<select className="input" name="clientType" defaultValue="" required><option value="" disabled>Selecciona una opción</option><option value="INDIVIDUAL">Persona física</option><option value="COMPANY">Empresa</option></select></label><label className="field">Servicio<select className="input" name="serviceSlug" defaultValue=""><option value="">Selecciona un servicio</option>{services.map((service) => <option key={service.slug} value={service.slug}>{service.name}</option>)}</select></label><label className="field">Estado<select className="input" name="state" defaultValue="" required><option value="" disabled>Selecciona tu estado</option><option>Ciudad de México</option><option>Estado de México</option><option>Jalisco</option><option>Nuevo León</option><option>Otro</option></select></label><label className="field sm:col-span-2">Cuéntanos sobre tu trámite<textarea className="input min-h-30 resize-y" name="message" required /></label><div className="sm:col-span-2"><button className="button" type="submit">Enviar solicitud</button><p className="mt-3 text-xs leading-5 text-slate-500">Al enviar, aceptas que te contactemos para atender tu solicitud.</p></div></form>; }
