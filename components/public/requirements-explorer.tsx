"use client";
import { useMemo, useState } from "react";
import { CheckCircle2, Search } from "lucide-react";
import { services, type Audience } from "@/lib/data/services";

export function RequirementsExplorer() { const [audience, setAudience] = useState<Audience>("persona"); const [query, setQuery] = useState(""); const filtered = useMemo(() => services.filter((service) => service.audiences.includes(audience) && service.name.toLowerCase().includes(query.toLowerCase())), [audience, query]); return <div><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div className="inline-flex rounded-xl bg-slate-100 p-1"><button className={`tab ${audience === "persona" ? "tab-active" : ""}`} onClick={() => setAudience("persona")}>Persona física</button><button className={`tab ${audience === "empresa" ? "tab-active" : ""}`} onClick={() => setAudience("empresa")}>Empresa</button></div><label className="relative block"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" size={18} /><span className="sr-only">Buscar trámite</span><input className="input pl-10" placeholder="Buscar trámite" value={query} onChange={(event) => setQuery(event.target.value)} /></label></div><div className="mt-7 grid gap-4 md:grid-cols-2">{filtered.map((service) => <article className="card p-6" key={service.slug}><h3 className="font-bold text-navy">{service.name}</h3><ul className="mt-4 space-y-3">{service.requirements[audience].map((requirement) => <li className="flex gap-2 text-sm leading-5 text-slate-600" key={requirement}><CheckCircle2 className="mt-0.5 shrink-0 text-blue" size={16} />{requirement}</li>)}</ul></article>)}</div>{filtered.length === 0 && <p className="py-12 text-center text-slate-500">No encontramos un trámite con ese nombre.</p>}</div>; }
