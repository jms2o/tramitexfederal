"use client";

import { Bar, BarChart, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";

type Props = { proceduresByMonth: { month: string; procedures: number }[]; proceduresByStatus: { name: string; value: number }[] };
const colors = ["#0B57D0", "#3B82F6", "#60A5FA", "#93C5FD", "#082E68", "#CBD5E1"];

export function DashboardCharts({ proceduresByMonth, proceduresByStatus }: Props) { return <div className="grid gap-6 xl:grid-cols-[1.25fr_.75fr]"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-bold text-navy">Trámites recibidos</h2><p className="mt-1 text-sm text-slate-500">Últimos seis meses</p><div className="mt-5 h-64"><ResponsiveContainer width="100%" height="100%"><BarChart data={proceduresByMonth}><XAxis dataKey="month" axisLine={false} tickLine={false} tick={{ fill: "#64748b", fontSize: 12 }} /><YAxis allowDecimals={false} axisLine={false} tickLine={false} tick={{ fill: "#64748b", fontSize: 12 }} /><Tooltip cursor={{ fill: "#eff6ff" }} contentStyle={{ borderRadius: 12, borderColor: "#e2e8f0" }} /><Bar dataKey="procedures" name="Trámites" fill="#0B57D0" radius={[6, 6, 0, 0]} /></BarChart></ResponsiveContainer></div></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-bold text-navy">Por estado</h2><p className="mt-1 text-sm text-slate-500">Distribución actual</p>{proceduresByStatus.length ? <div className="mt-4 h-64"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={proceduresByStatus} dataKey="value" nameKey="name" innerRadius={58} outerRadius={85} paddingAngle={3}>{proceduresByStatus.map((entry, index) => <Cell key={entry.name} fill={colors[index % colors.length]} />)}</Pie><Tooltip contentStyle={{ borderRadius: 12, borderColor: "#e2e8f0" }} /></PieChart></ResponsiveContainer></div> : <div className="grid h-64 place-items-center text-center text-sm text-slate-500">Aún no hay trámites registrados.</div>}</section></div>; }
