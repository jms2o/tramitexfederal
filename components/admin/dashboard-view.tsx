import Link from "next/link";
import { Activity, CircleDollarSign, Clock3, FileClock, UsersRound, Workflow } from "lucide-react";
import { DashboardCharts } from "@/components/admin/dashboard-charts";

type DashboardData = Awaited<ReturnType<typeof import("@/lib/db/dashboard").getDashboardData>>;
const currency = new Intl.NumberFormat("es-MX", { style: "currency", currency: "MXN", maximumFractionDigits: 0 });

export function DashboardView({ data }: { data: DashboardData }) { const cards = [{ label: "Trámites activos", value: data.metrics.activeProcedures, icon: Workflow, href: "/admin/expedientes?filter=active" }, { label: "Esperando documentos", value: data.metrics.waitingDocuments, icon: FileClock, href: "/admin/expedientes?status=WAITING_DOCUMENTS" }, { label: "En revisión", value: data.metrics.underReview, icon: Clock3, href: "/admin/expedientes?status=UNDER_REVIEW" }, { label: "En proceso", value: data.metrics.inProgress, icon: Activity, href: "/admin/expedientes?status=IN_PROGRESS" }, { label: "Finalizados", value: data.proceduresByStatus.find((item) => item.name === "Finalizado")?.value ?? 0, icon: Workflow, href: "/admin/expedientes?status=COMPLETED" }, { label: "Clientes registrados", value: data.metrics.clients, icon: UsersRound, href: "/admin/expedientes" }, { label: "Ingresos del mes", value: currency.format(data.metrics.monthlyIncome), icon: CircleDollarSign, href: "/admin/cotizaciones" }]; return <div className="space-y-8"> <div><p className="eyebrow">Vista general</p><h1 className="mt-2 text-3xl font-bold tracking-tight text-navy">Dashboard</h1><p className="mt-2 text-sm text-slate-600">Resumen operativo de TramitexFederal.</p></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{cards.map((card) => { const Icon = card.icon; return <Link href={card.href} className="card card-action p-5" key={card.label}><span className="grid size-10 place-items-center rounded-xl bg-blue-pale text-blue"><Icon size={20} /></span><p className="mt-5 text-2xl font-bold text-navy">{card.value}</p><p className="mt-1 text-sm text-slate-500">{card.label}</p></Link>; })}</div><DashboardCharts proceduresByMonth={data.proceduresByMonth} proceduresByStatus={data.proceduresByStatus} /><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-bold text-navy">Actividad reciente</h2>{data.activity.length ? <ul className="mt-4 divide-y divide-slate-100">{data.activity.map((activity) => <li key={activity.id} className="flex items-center justify-between gap-4 py-3"><div><p className="text-sm font-medium text-slate-700">{activity.action}</p><p className="mt-1 text-xs text-slate-500">{activity.entityType} · {activity.userName}</p></div><time className="text-xs text-slate-400" dateTime={activity.createdAt}>{new Intl.DateTimeFormat("es-MX", { dateStyle: "medium" }).format(new Date(activity.createdAt))}</time></li>)}</ul> : <p className="mt-4 text-sm text-slate-500">La actividad del equipo aparecerá aquí.</p>}</section></div>; }
