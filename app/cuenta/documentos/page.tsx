import { FileText } from "lucide-react";
import { getClientDocuments } from "@/lib/db/client-portal";

export default async function ClientDocumentsPage() { const documents = await getClientDocuments(); return <div><p className="eyebrow">Archivos privados</p><h1 className="mt-2 text-3xl font-bold tracking-tight text-navy">Mis documentos</h1><div className="mt-8 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">{documents.length ? <div className="overflow-x-auto"><table className="w-full min-w-160 text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-4">Archivo</th><th className="px-5 py-4">Trámite</th><th className="px-5 py-4">Estado</th><th className="px-5 py-4" /></tr></thead><tbody className="divide-y divide-slate-100">{documents.map((document) => <tr key={document.id}><td className="px-5 py-4 font-semibold text-navy">{document.originalName}<p className="mt-1 text-xs font-normal text-slate-500">{document.category}</p></td><td className="px-5 py-4 text-slate-600">{document.procedures[0]?.procedure.folio ?? "-"}</td><td className="px-5 py-4"><span className="rounded-full bg-blue-pale px-2.5 py-1 text-xs font-semibold text-blue">{document.status}</span></td><td className="px-5 py-4"><a className="font-semibold text-blue" href={`/api/documents/${document.id}/download`}>Descargar</a></td></tr>)}</tbody></table></div> : <div className="grid min-h-64 place-items-center p-8 text-center"><div><FileText className="mx-auto text-slate-300" size={34} /><p className="mt-4 text-sm text-slate-500">Aún no has cargado documentos.</p></div></div>}</div></div>; }
