import Link from "next/link";
import { ArrowLeft } from "lucide-react";
import { registerPayment } from "@/app/admin/actions";
import { clientLabel, getFinanceFormData } from "@/lib/db/finance";
export const metadata = { title: "Agregar pago" };
export default async function NewPaymentPage() { const procedures = await getFinanceFormData(); return <div className="max-w-3xl"><Link href="/admin/pagos" className="inline-flex items-center gap-2 text-sm font-semibold text-slate-600 hover:text-blue"><ArrowLeft size={16} />Volver a pagos</Link><h1 className="mt-5 text-3xl font-bold text-navy">Agregar pago</h1><form action={registerPayment} className="mt-8 grid gap-5 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:grid-cols-2"><label className="field">Cliente<select className="input" name="clientId" required defaultValue=""><option value="" disabled>Selecciona un cliente</option>{procedures.map((procedure) => <option key={procedure.id} value={procedure.clientId}>{clientLabel(procedure.client)}</option>)}</select></label><label className="field">Trámite<select className="input" name="procedureId" required defaultValue=""><option value="" disabled>Selecciona un trámite</option>{procedures.map((procedure) => <option key={procedure.id} value={procedure.id}>{procedure.folio} · {clientLabel(procedure.client)}</option>)}</select></label><label className="field">Monto<input className="input" name="amount" type="number" min="0.01" step="0.01" placeholder="0.00" required /></label><label className="field">Método de pago<select className="input" name="method" defaultValue="TRANSFER"><option value="CASH">Efectivo</option><option value="TRANSFER">Transferencia</option><option value="CARD">Tarjeta</option><option value="OTHER">Otro</option></select></label><label className="field">Referencia<input className="input" name="reference" /></label><label className="field">Fecha de registro<input className="input" type="date" defaultValue={new Date().toISOString().slice(0, 10)} /></label><label className="field sm:col-span-2">Notas <span className="font-normal text-slate-400">(el modelo actual no conserva notas por pago)</span><textarea className="input min-h-24" disabled /></label><div className="flex justify-end gap-3 border-t border-slate-100 pt-6 sm:col-span-2"><Link className="button button-outline" href="/admin/pagos">Cancelar</Link><button className="button" type="submit">Registrar pago</button></div></form></div>; }
