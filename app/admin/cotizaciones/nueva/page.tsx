import Link from "next/link";
import { ArrowLeft } from "lucide-react";
import { saveQuote } from "@/app/admin/actions";
import { clientLabel, getFinanceFormData } from "@/lib/db/finance";
export const metadata = { title: "Nueva cotización" };
export default async function NewQuotePage() { const procedures = await getFinanceFormData(); return <div className="max-w-3xl"><Link href="/admin/cotizaciones" className="inline-flex items-center gap-2 text-sm font-semibold text-slate-600 hover:text-blue"><ArrowLeft size={16} />Volver a cotizaciones</Link><h1 className="mt-5 text-3xl font-bold text-navy">Nueva cotización</h1><form action={saveQuote} className="mt-8 grid gap-5 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:grid-cols-2"><label className="field">Cliente<select className="input" name="clientId" required defaultValue=""><option value="" disabled>Selecciona un cliente</option>{procedures.map((procedure) => <option key={procedure.id} value={procedure.clientId}>{clientLabel(procedure.client)}</option>)}</select></label><label className="field">Trámite relacionado<select className="input" name="procedureId" required defaultValue=""><option value="" disabled>Selecciona un trámite</option>{procedures.map((procedure) => <option key={procedure.id} value={procedure.id}>{procedure.folio} · {clientLabel(procedure.client)}</option>)}</select></label><label className="field sm:col-span-2">Conceptos <span className="font-normal text-slate-400">(uno por línea: concepto | monto)</span><textarea className="input min-h-32" name="items" placeholder={"Honorarios del servicio | 2500.00\nDerechos | 1200.00"} required /></label><label className="field">Impuestos<input className="input" name="tax" type="number" min="0" step="0.01" defaultValue="0" required /></label><label className="field sm:col-span-2">Notas<textarea className="input min-h-24" name="notes" /></label><div className="flex justify-end gap-3 border-t border-slate-100 pt-6 sm:col-span-2"><Link className="button button-outline" href="/admin/cotizaciones">Cancelar</Link><button className="button" type="submit">Guardar cotización</button></div></form></div>; }
