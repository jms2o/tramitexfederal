import Link from "next/link";
import { FileText, Plus } from "lucide-react";
import { clientLabel, getQuotes } from "@/lib/db/finance";

const currency = new Intl.NumberFormat("es-MX", { style: "currency", currency: "MXN" });
export const metadata = { title: "Cotizaciones" };
export default async function QuotesPage() { const quotes = await getQuotes(); return <div><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">Finanzas</p><h1 className="mt-2 text-3xl font-bold tracking-tight text-navy">Cotizaciones</h1><p className="mt-2 text-sm text-slate-600">Cotizaciones, pagos y saldo de cada expediente.</p></div><Link className="button" href="/admin/cotizaciones/nueva"><Plus size={17} />Nueva cotización</Link></div><section className="mt-8 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">{quotes.length ? <div className="overflow-x-auto"><table className="w-full min-w-220 text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-4">Folio</th><th className="px-5 py-4">Cliente</th><th className="px-5 py-4">Servicio</th><th className="px-5 py-4">Total</th><th className="px-5 py-4">Pagado</th><th className="px-5 py-4">Saldo</th></tr></thead><tbody className="divide-y divide-slate-100">{quotes.map((quote) => { const paid = quote.procedure.payments.reduce((sum, payment) => sum + Number(payment.amount), 0); const total = Number(quote.total); return <tr key={quote.id}><td className="px-5 py-4"><Link href={`/admin/cotizaciones/${quote.id}`} className="font-semibold text-blue hover:text-navy">{quote.procedure.folio}</Link></td><td className="px-5 py-4 text-slate-600">{clientLabel(quote.procedure.client)}</td><td className="px-5 py-4 text-slate-600">{quote.procedure.service.name}</td><td className="px-5 py-4 font-semibold text-navy">{currency.format(total)}</td><td className="px-5 py-4 text-slate-600">{currency.format(paid)}</td><td className="px-5 py-4 font-semibold text-navy">{currency.format(Math.max(0, total - paid))}</td></tr>; })}</tbody></table></div> : <div className="grid min-h-70 place-items-center p-8 text-center"><div><FileText className="mx-auto text-slate-300" size={34} /><p className="mt-4 font-semibold text-navy">Aún no hay cotizaciones</p><p className="mt-2 text-sm text-slate-500">Crea una cotización vinculada a un expediente.</p><Link className="button mt-5" href="/admin/cotizaciones/nueva">Nueva cotización</Link></div></div>}</section></div>; }
