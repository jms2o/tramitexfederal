import Link from "next/link";
import { ArrowLeft } from "lucide-react";
import { createVehicle } from "@/app/admin/actions";
import { getClientOptions } from "@/lib/db/crm";

export const metadata = { title: "Nuevo vehículo" };

export default async function NewVehiclePage(props: PageProps<"/admin/vehiculos/nuevo">) { const [clients, params] = await Promise.all([getClientOptions(), props.searchParams]); const message = typeof params.error === "string" ? params.error : null; return <div className="max-w-3xl"><Link href="/admin/vehiculos" className="inline-flex items-center gap-2 text-sm font-semibold text-slate-600 hover:text-blue"><ArrowLeft size={16} />Volver a vehículos</Link><h1 className="mt-5 text-3xl font-bold tracking-tight text-navy">Registrar vehículo</h1><p className="mt-2 text-sm text-slate-600">Vincula una unidad a un cliente existente.</p>{message && <p className="mt-6 rounded-xl bg-red-50 p-4 text-sm text-red-700" role="alert">{message}</p>}<form action={createVehicle} className="mt-8 grid gap-5 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:grid-cols-2"><label className="field sm:col-span-2">Cliente<select className="input" name="clientId" required defaultValue=""><option value="" disabled>Selecciona un cliente</option>{clients.map((client) => <option key={client.id} value={client.id}>{client.company?.legalName ?? `${client.firstName ?? ""} ${client.lastName ?? ""}`.trim()}</option>)}</select></label><label className="field">Marca<input className="input" name="brand" required /></label><label className="field">Modelo<input className="input" name="model" required /></label><label className="field">Año<input className="input" name="year" type="number" min="1900" max={new Date().getFullYear() + 1} required /></label><label className="field">NIV / serie<input className="input" name="vin" /></label><label className="field">Placas federales<input className="input" name="federalPlate" /></label><label className="field">Referencia de factura<input className="input" name="invoiceRef" /></label><label className="field sm:col-span-2">Referencia de póliza<input className="input" name="insuranceRef" /></label><div className="flex justify-end gap-3 border-t border-slate-100 pt-6 sm:col-span-2"><Link className="button button-outline" href="/admin/vehiculos">Cancelar</Link><button className="button" type="submit">Guardar vehículo</button></div></form></div>; }
