import Link from "next/link";
import { Building2, Plus } from "lucide-react";
import { getCompanies } from "@/lib/db/crm";

export const metadata = { title: "Empresas" };
export default async function CompaniesPage() { const companies = await getCompanies(); return <div><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">CRM</p><h1 className="mt-2 text-3xl font-bold tracking-tight text-navy">Empresas y socios</h1><p className="mt-2 text-sm text-slate-600">Información corporativa y representantes registrados.</p></div><Link className="button" href="/admin/clientes/nuevo"><Plus size={17} />Nueva empresa</Link></div><div className="mt-8 grid gap-5 lg:grid-cols-2">{companies.map((company) => <article className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm" key={company.id}><div className="flex items-start justify-between gap-3"><div><h2 className="font-bold text-navy">{company.legalName}</h2><p className="mt-1 text-sm text-slate-500">{company.rfc}</p></div><span className="rounded-full bg-blue-pale px-2.5 py-1 text-xs font-semibold text-blue">Empresa</span></div><dl className="mt-5 grid gap-3 text-sm"><div><dt className="text-slate-400">Contacto</dt><dd className="font-medium text-slate-700">{company.client.phone ?? company.client.email ?? "Sin contacto"}</dd></div><div><dt className="text-slate-400">Socios</dt><dd className="font-medium text-slate-700">{company.partners.length ? company.partners.map((partner) => partner.fullName).join(", ") : "Sin socios registrados"}</dd></div></dl></article>)}{!companies.length && <div className="grid min-h-70 place-items-center rounded-2xl border border-dashed border-slate-300 p-8 text-center lg:col-span-2"><div><Building2 className="mx-auto text-slate-300" size={34} /><p className="mt-4 font-semibold text-navy">Aún no hay empresas</p><Link className="button mt-5" href="/admin/clientes/nuevo">Registrar empresa</Link></div></div>}</div></div>; }
