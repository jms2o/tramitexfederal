import Link from "next/link";
import { ArrowLeft } from "lucide-react";
import { createClient } from "@/app/admin/actions";

export const metadata = { title: "Nuevo cliente" };

export default async function NewClientPage(props: PageProps<"/admin/clientes/nuevo">) { const { error } = await props.searchParams; const message = typeof error === "string" ? error : null; return <div className="max-w-6xl"><Link href="/admin/expedientes" className="inline-flex items-center gap-2 text-sm font-semibold text-slate-600 hover:text-blue"><ArrowLeft size={16} />Volver a expedientes</Link><h1 className="mt-5 text-3xl font-bold tracking-tight text-navy">Registrar cliente</h1><p className="mt-2 text-sm text-slate-600">Completa los datos personales y, cuando aplique, los datos fiscales de la empresa.</p>{message && <p className="mt-6 rounded-xl bg-red-50 p-4 text-sm text-red-700" role="alert">{message}</p>}<form action={createClient} className="mt-8 grid gap-6"><div className="grid gap-6 lg:grid-cols-2"><fieldset className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><legend className="mb-4 font-bold text-navy">Datos del cliente</legend><div className="grid gap-5 sm:grid-cols-2"><label className="field">Tipo de cliente<select className="input" name="type" defaultValue="INDIVIDUAL"><option value="INDIVIDUAL">Persona física</option><option value="COMPANY">Empresa</option></select></label><label className="field">Teléfono<input className="input" name="phone" required /></label><label className="field">Nombre<input className="input" name="firstName" /></label><label className="field">Apellidos<input className="input" name="lastName" /></label><label className="field">Correo<input className="input" name="email" type="email" /></label><label className="field">RFC<input className="input" name="rfc" /></label><label className="field sm:col-span-2">Domicilio<textarea className="input min-h-24" name="address" /></label></div></fieldset><fieldset className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><legend className="mb-4 font-bold text-navy">Empresa / datos fiscales <span className="font-normal text-slate-400">(solo si aplica)</span></legend><div className="grid gap-5 sm:grid-cols-2"><label className="field sm:col-span-2">Razón social<input className="input" name="legalName" /></label><label className="field">Nombre comercial<input className="input" name="tradeName" /></label><label className="field">Representante legal<input className="input" name="legalRepresentative" /></label><label className="field sm:col-span-2">Socios <span className="font-normal text-slate-400">(un nombre por línea)</span><textarea className="input min-h-28" name="partnerNames" /></label></div></fieldset></div><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-bold text-navy">Trámites del cliente</h2><p className="mt-2 text-sm text-slate-600">Guarda primero el cliente para asignarle un expediente de forma segura. Podrás crear el primer trámite inmediatamente después.</p></section><div className="flex justify-end gap-3"><Link className="button button-outline" href="/admin/expedientes">Cancelar</Link><button className="button" type="submit">Guardar cliente</button></div></form></div>; }
