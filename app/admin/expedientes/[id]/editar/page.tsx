import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft } from "lucide-react";
import { updateClient } from "@/app/admin/actions";
import { getClientExpedient } from "@/lib/db/crm";

export const metadata = { title: "Editar expediente" };
export default async function EditExpedientPage(props: { params: Promise<{ id: string }>; searchParams: Promise<{ error?: string }> }) {
  const [{ id }, params] = await Promise.all([props.params, props.searchParams]); const client = await getClientExpedient(id); if (!client) notFound();
  return <div className="max-w-6xl"><Link href={`/admin/expedientes/${client.id}`} className="inline-flex items-center gap-2 text-sm font-semibold text-slate-600 hover:text-blue"><ArrowLeft size={16} />Volver al expediente</Link><h1 className="mt-5 text-3xl font-bold tracking-tight text-navy">Editar expediente</h1><p className="mt-2 text-sm text-slate-600">Actualiza los datos del cliente sin modificar su historial de trámites, documentos o pagos.</p>{params.error && <p className="mt-5 rounded-xl bg-red-50 p-4 text-sm text-red-700" role="alert">{params.error}</p>}<form action={updateClient} className="mt-8 grid gap-6"><input type="hidden" name="clientId" value={client.id} /><div className="grid gap-6 lg:grid-cols-2"><fieldset className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><legend className="mb-4 font-bold text-navy">Datos del cliente</legend><div className="grid gap-5 sm:grid-cols-2"><label className="field">Tipo de cliente<select className="input" name="type" defaultValue={client.type}><option value="INDIVIDUAL">Persona física</option><option value="COMPANY">Empresa</option></select></label><label className="field">Teléfono<input className="input" name="phone" defaultValue={client.phone ?? ""} required /></label><label className="field">Nombre<input className="input" name="firstName" defaultValue={client.firstName ?? ""} /></label><label className="field">Apellidos<input className="input" name="lastName" defaultValue={client.lastName ?? ""} /></label><label className="field">Correo<input className="input" name="email" type="email" defaultValue={client.email ?? ""} /></label><label className="field">RFC<input className="input" name="rfc" defaultValue={client.rfc ?? client.company?.rfc ?? ""} /></label><label className="field sm:col-span-2">Domicilio<textarea className="input min-h-24" name="address" defaultValue={client.address ?? ""} /></label></div></fieldset><fieldset className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><legend className="mb-4 font-bold text-navy">Empresa / datos fiscales</legend><div className="grid gap-5 sm:grid-cols-2"><label className="field sm:col-span-2">Razón social<input className="input" name="legalName" defaultValue={client.company?.legalName ?? ""} /></label><label className="field">Nombre comercial<input className="input" name="tradeName" defaultValue={client.company?.tradeName ?? ""} /></label><label className="field">Representante legal<input className="input" name="legalRepresentative" defaultValue={client.company?.legalRepresentative ?? ""} /></label><label className="field sm:col-span-2">Socios <span className="font-normal text-slate-400">(un nombre por línea)</span><textarea className="input min-h-28" name="partnerNames" defaultValue={client.company?.partners.map((partner) => partner.fullName).join("\n") ?? ""} /></label></div></fieldset></div><div className="flex justify-end gap-3"><Link className="button button-outline" href={`/admin/expedientes/${client.id}`}>Cancelar</Link><button className="button" type="submit">Guardar cambios</button></div></form></div>;
}
