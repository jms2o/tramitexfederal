import Link from "next/link";
import { ArrowLeft } from "lucide-react";
import { createProcedure } from "@/app/admin/actions";
import { getProcedureFormData } from "@/lib/db/procedures";

export const metadata = { title: "Nuevo trámite" };
export default async function NewProcedurePage(props: PageProps<"/admin/tramites/nuevo">) { const [data, params] = await Promise.all([getProcedureFormData(), props.searchParams]); const error = typeof params.error === "string" ? params.error : null; return <div className="max-w-3xl"><Link href="/admin/tramites" className="inline-flex items-center gap-2 text-sm font-semibold text-slate-600 hover:text-blue"><ArrowLeft size={16} />Volver a trámites</Link><h1 className="mt-5 text-3xl font-bold tracking-tight text-navy">Crear expediente</h1><p className="mt-2 text-sm text-slate-600">Al guardar se genera un folio único y una copia del checklist del servicio.</p>{error && <p className="mt-6 rounded-xl bg-red-50 p-4 text-sm text-red-700" role="alert">{error}</p>}<form action={createProcedure} className="mt-8 grid gap-5 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:grid-cols-2"><label className="field sm:col-span-2">Cliente<select className="input" name="clientId" defaultValue="" required><option value="" disabled>Selecciona un cliente</option>{data.clients.map((client) => <option key={client.id} value={client.id}>{client.company?.legalName ?? `${client.firstName ?? ""} ${client.lastName ?? ""}`.trim()}</option>)}</select></label><label className="field">Servicio<select className="input" name="serviceId" defaultValue="" required><option value="" disabled>Selecciona un servicio</option>{data.services.map((service) => <option key={service.id} value={service.id}>{service.name}</option>)}</select></label><label className="field">Vehículo <span className="font-normal text-slate-400">(opcional)</span><select className="input" name="vehicleId" defaultValue=""><option value="">Sin vehículo</option>{data.vehicles.map((vehicle) => <option key={vehicle.id} value={vehicle.id}>{vehicle.brand} {vehicle.model} ({vehicle.year})</option>)}</select></label><label className="field sm:col-span-2">Mensaje público inicial <span className="font-normal text-slate-400">(opcional)</span><textarea className="input min-h-24" name="publicMessage" /></label><label className="field sm:col-span-2">Notas internas <span className="font-normal text-slate-400">(opcional)</span><textarea className="input min-h-28" name="internalNotes" /></label><div className="flex justify-end gap-3 border-t border-slate-100 pt-6 sm:col-span-2"><Link className="button button-outline" href="/admin/tramites">Cancelar</Link><button className="button" type="submit">Crear expediente</button></div></form></div>; }
